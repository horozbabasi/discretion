/**
 * Package entry point.
 *
 * The extension is not a library, so this exists for the type-checked build
 * and for tests to import from one place rather than reaching into file paths.
 */

export * from './adapters/index.js';
export * from './messages.js';
